
SUBDIRS(teuchos)

IF(WITH_FFT)
#  SUBDIRS(dfftpack fftw)
  SUBDIRS(dfftpack)
ELSEIF(HAVE_DFFTPACK)
  SUBDIRS(dfftpack)
ELSEIF(WITH_FFTW)
  SUBDIRS(fftw)
ENDIF(WITH_FFT)

IF(HAVE_LHS)
  SUBDIRS(LHS)
ENDIF(HAVE_LHS)

IF(HAVE_SPARSE_GRID)
  SUBDIRS(VPISparseGrid)
ENDIF(HAVE_SPARSE_GRID)

