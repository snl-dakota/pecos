// $Header: 

/* CMake has a better way ("prefix" the name name to avoid clash)
// WJB:  HOW do I leverage the concept WITHOUT breaking the autotools build?
//       Zack tested an "empty" header idea in opt++ and it seems to work
//
// Define to a macro mangling the given C identifier (in lower and upper
// case), which must not contain underscores, for linking with Fortran. 
#define FC_FUNC@FortranCInterface_GLOBAL_MACRO@

// As FC_FUNC, but for C identifiers containing underscores.
#define FC_FUNC_@FortranCInterface_GLOBAL_MACRO@

// Macro to enable LHS.
// #cmakedefine HAVE_LHS 1
*/

// Macro to enable debug in Pecos.
// #cmakedefine HAVE_PECOS_DEBUG

